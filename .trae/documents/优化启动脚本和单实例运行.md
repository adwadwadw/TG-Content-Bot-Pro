1. **修改启动脚本，添加代理环境变量设置**：

   * 修改 `main/app.py` 或创建独立启动脚本

   * 检查 `USE_PROXY` 配置

   * 如果启用代理，将代理配置添加到系统环境变量

   * 确保所有客户端使用相同的代理配置

2. **实现单实例运行机制**：

   * 使用文件锁机制确保程序只能单个运行

   * 在程序启动时创建锁文件

   * 检测到已有锁文件时，终止旧实例

   * 程序退出时释放锁文件

3. **测试优化效果**：

   * 验证代理配置是否正确添加到环境变量

   * 测试单实例运行机制是否工作正常

   * 确保程序启动和退出时锁文件正确创建和删除

