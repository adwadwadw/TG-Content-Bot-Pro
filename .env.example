# 环境变量配置示例
# 复制此文件为 .env 并填入您的真实配置

# Telegram API 凭证（从 my.telegram.org 获取）
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# Bot Token（从 @BotFather 获取）
BOT_TOKEN=your_bot_token_here

# Pyrogram 会话字符串（可选，运行 get_session.py 获取）
# 如果配置了 MongoDB，可以通过 /addsession 命令在机器人中添加
SESSION=

# 机器人所有者的 Telegram 用户 ID（从 @userinfobot 获取）
AUTH=your_user_id_here

# 强制订阅频道用户名（可选，不带 @ 符号）
FORCESUB=

# MongoDB 连接字符串（可选，用于存储 SESSION 和流量数据）
# 示例：mongodb://username:password@host:port/database
# 或 MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
MONGO_DB=

# 加密密钥（用于加密存储 SESSION 字符串）
# 运行时会自动生成，建议复制到此处以保持密钥一致
ENCRYPTION_KEY=



# 日志配置（可选）
# LOG_LEVEL - 控制日志输出的详细程度，默认为INFO
# LOG_FILE  - 如果设置，将日志写入指定文件（默认由程序生成每日序号文件）
# 可选值（从最详细到最简略）：
#   DEBUG    - 最详细的日志信息，用于开发和故障排除
#   INFO     - 一般信息性消息，跟踪应用程序的正常运行状态（默认）
#   WARNING  - 警告信息，表示可能有问题但不会影响程序继续运行
#   ERROR    - 错误信息，表示发生了错误但程序仍可继续运行
#   CRITICAL - 严重错误信息，表示发生了严重影响程序运行的错误
# 示例：设置为WARNING级别将只显示警告和错误信息
# LOG_LEVEL=INFO
# LOG_FILE=bot.log

# 并发配置（可选）
# MAX_WORKERS      - 初始工作线程数（默认2）
# MIN_CONCURRENCY  - 自适应并发下限（默认1）
# MAX_CONCURRENCY  - 自适应并发上限（默认10）
# 示例：
# MAX_WORKERS=2
# MIN_CONCURRENCY=1
# MAX_CONCURRENCY=10